[project]
name = "seros-spike-python"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "django>=6.1",
    "django-tasks-db>=0.12.0",
    "pydantic>=2.13.4",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-django>=4.14.0",
]
