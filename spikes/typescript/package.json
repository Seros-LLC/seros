{
  "name": "seros-spike-typescript",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "migrate": "node --experimental-transform-types src/db/migrate.ts",
    "start": "node --experimental-transform-types src/main.ts",
    "worker": "node --experimental-transform-types src/queue/worker-main.ts",
    "smoke": "node --experimental-transform-types src/smoke.ts",
    "eval": "node --experimental-transform-types src/eval/run-eval.ts",
    "tenant-lint": "node --experimental-transform-types scripts/tenant-lint.ts",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && npm run tenant-lint && node --experimental-transform-types --test test/*.test.ts"
  },
  "dependencies": {
    "@fastify/formbody": "^9.0.0",
    "better-sqlite3": "^13.0.3",
    "drizzle-orm": "^0.45.2",
    "fastify": "^5.12.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^26.2.0",
    "drizzle-kit": "^0.31.10",
    "typescript": "^7.0.2"
  }
}
